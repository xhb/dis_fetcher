require 'spec_helper'

describe DisFetcher do
  it 'has a version number' do
    expect(DisFetcher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
